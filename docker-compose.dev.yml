# 开发覆盖：docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build
services:
  api:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./backend/app:/app/app
      - ./configs:/app/configs:ro
      - market_data:/app/data
