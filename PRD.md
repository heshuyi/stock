# A股宽基定投投顾看板 — 产品需求文档（PRD）

版本：v6（组合再平衡为研究开关，默认关闭；在 v5 对账/复盘/提醒之上）  
状态：已实现 / 与代码对齐 / 再平衡未过默认启用门槛

---

## 1. 产品定位

个人本地投顾看板。面向 A 股宽基 ETF，按用户设定的**月定投总预算**与**定投频率（每日 / 每周 / 每月）**，在每个执行日给出操作清单：本期分配额度、暂停、减仓，并展示分角色策略明细。分配额度用于策略预算分配，不等于按 ETF 一手规则取整后的券商可下单金额。

### 用户故事

1. 作为长期定投用户，我设置每月预算 3000 元，并选择每周一或每月 1 日定投，系统自动算出本期该投多少钱。  
2. 作为风控敏感用户，我希望成长仓在空头排列时暂停买入，核心仓仅降频不断粮。  
3. 作为执行用户，我打开看板就能知道「今天要不要买、买多少、为何暂停」。

---

## 2. 覆盖标的与目标权重

| 代号 | 标的（中文全称+证券代码） | 目标权重 | 角色 |
|------|---------------------------|----------|------|
| HS300 | 华泰柏瑞沪深300ETF（510300） | 35% | 大盘蓝筹核心 |
| ZZ500 | 南方中证500ETF（510500） | 25% | 中盘成长 |
| CYB200 | 景顺长城创业板200ETF（159572） | 15% | 中盘成长弹性 |
| KCB50 | 华夏上证科创板50ETF（588000） | 10% | 硬科技成长 |
| SZ50 | 华夏上证50ETF（510050） | 15% | 超大盘防守 |

界面展示统一为「中文全称（证券代码）」。

创业板200 / 科创50 使用板块代理 PE/PB，信号必须标注代理口径；科创50 估值分位用全样本窗口。

---

## 3. 分角色策略（v2）

### 3.1 合成权重与算子

参数按 **两套角色模板** 配置（见 `configs/symbols.json`），避免五标的各写一套：

| 模板 | 标的 | 合成权重 |
|------|------|----------|
| 核心 | HS300、ZZ500、SZ50 | 估值 70% + 趋势 30% |
| 成长 | CYB200、KCB50 | 估值 55% + 趋势 45% |

科创50 仅额外使用 `percentile_window=full` 与板块代理估值；其余成长参数与创业板200 对齐。

**合成算子：加权平均（不是相乘）。**

\[
final\_mult = \mathrm{clip}(w_v \cdot m_{val} + w_t \cdot m_{trend},\; 0,\; max\_mult)
\]

**可达上限缩放（v4）**：加权平均的天然上限 = 估值最高档 × 估值权重 + 多头 × 趋势权重（核心 0.7×1.8+0.3×1.0=1.56；成长 0.55×1.6+0.45×1.0=1.33），导致 `max_mult`（2.0）永远无法触达、预算上限形同虚设。现在当 profile `scale_to_cap=true` 时，合成结果按 `max_mult / 可达上限` 等比放大（核心 ×1.282、成长 ×1.504），使「低估 + 多头」真正达到 2.0×，`normalize_buy_cap` 预算上限成为有效安全阀。放大不改变加权平均的形态（仍非相乘），也不作用于硬否决/超跌解封路径。

产品取舍：

| 方案 | 行为 | 为何采用/不用 |
|------|------|----------------|
| **加权平均（现行）** | 估值决定主加码，趋势软降频 | 符合「低估仍愿投、破位只减额」的定投意图 |
| 相乘 \(m_{val}\times m_{trend}\) | 两侧同时偏低会过度打折 | 易把「低估 + 夹层/偏弱趋势」压成接近 1× 或更低，核心仓加码不足 |
| 更复杂打分/ML | 难解释、难验收 | 宽基定投边际收益主要来自阈值与角色，不来自合成算子 |

真正的一票否决用 **硬否决 → 倍数 0**（见 3.4），不必再用乘法叠一层闸门。止盈**不进入**买入倍数加权，只产出减仓建议。

### 3.1.1 止盈阈值单轨

- **武装线 / 清仓线**：以设置页全局 `valuation_reduce_percentile` / `valuation_exit_percentile` 为准（引擎覆盖 profile）。  
- **追踪回撤幅度** `trail_drawdown`：按角色模板（核心 10%，成长 8%）。  
- **清仓锁与再入场（v4）**：进入清仓级（stage 2）后，DCA 不再继续买入，直到估值回落至 `清仓线 − reentry_gap`（默认 0.1）以下才恢复新增；否则「转入低风险现金管理」会被持续的定投悄悄推翻。锁只抑制买入，不抑制止盈减仓建议。
- 配置里不再为各标的单独写武装/清仓分位，避免与设置页双轨冲突。

### 3.2 估值

- 核心：PE 分位为主；成长：0.55×PE + 0.45×PB 复合。  
- 各标的 pause 阈值与倍数档见 `configs/symbols.json` → `strategy_profile`。  
- 估值档位边界 `band_low/band_mid/band_high`（默认 0.2/0.4/0.6）可配置，须满足 `0 ≤ band_low ≤ band_mid ≤ band_high < pause_percentile`，避免档位与暂停阈值错位。  
- `5y` 分位窗口按信号日向前回溯 **5 个自然年**，不以 252 日近似；样本少于 5 个时视为数据缺失。  
- 估值日期与价格日期分别管理；估值最多允许滞后 5 个 XSHG 交易日，超过阈值按数据缺失安全暂停新增，不解释为“估值过高”。

### 3.3 趋势

- 状态：多头 / 偏多 / 夹层 / 空头排列（价与 MA60 均在 MA120 下）。  
- 核心：无趋势硬否决，破位仍可小额定投；±1% Bias 滞回。  
- 成长：空头排列硬停；若复合估值 p&lt;15% 且 Bias&lt;-12%，超跌解封 0.25×。  
- **软降频选项（v4，用户可切换）**：设置页「成长仓空头策略」二选一——`hard_veto`（防守，默认，空头排列硬停）或 `soft`（追收益，按 `growth_bear_mult` 默认 0.2 小额续投，倍数可调 0–1）；该开关同时作用于创业板200 / 科创50。回测显示这是当前策略最大的单因子收益杠杆（全样本 XIRR 3.25%→4.51%），也是「防守定位 vs 追收益」的核心取舍旋钮。

### 3.4 硬否决

估值 pause（或数据缺失）**或**（成长仓空头排列且未解封、且未启用软降频）→ 买入倍数 0。清仓止盈锁（3.1.1）同样抑制新增买入，但**不覆盖**止盈减仓建议。

### 3.5 止盈

估值武装 + 峰值追踪回撤；不用账户收益率硬触发。止盈**每日检查**。

### 3.6 现金池

现金池由 `cash_pool_enabled` 显式控制，默认关闭。关闭时 `pool_factor=1.0`；启用时：

`pool_factor = clip(cash / (base_amount × 36), 0, 1.25)`

启用后的 `cash` 是真实可支配余额，**`cash=0` 表示弹药为空，pool_factor 归 0、本期暂停买入**（v4 起不再有 0.35× 下限，避免「弹药为空仍建议买入」的矛盾）；止盈减仓建议不受影响。`pool_factor` 就是最终缩放系数，不再叠加隐藏折扣。

---

## 4. 定投调度与金额引擎（v3 核心）

### 4.1 字段

| 字段 | 含义 | 默认 |
|------|------|------|
| `base_amount` | **月定投总预算**（元），不随频率改变含义 | 配置默认；用户可改为 3000 |
| `buy_frequency` | `daily` / `weekly` / `monthly` | `monthly` |
| `weekly_weekday` | 1=周一 … 5=周五 | 1 |
| `monthly_day` | 每月目标日 1–28 | 1 |

### 4.2 本期基准额 `period_amount`

| 频率 | 公式 |
|------|------|
| 每日 | `base_amount / N_month`，`N_month` = 当月交易日数 |
| 每周 | `base_amount / W_month`，`W_month` = 当月含 ≥1 个交易日的 ISO 自然周数 |
| 每月 | `base_amount` |

`N_month` / `W_month` 均按 XSHG 官方完整当月日历计算，不随月内日期变化；每日/每周结果向下取整到分，确保整月计划额度不超过月预算。

单标的本期分配额度：

`amount = period_amount × target_weight × final_mult`

预算上限与现金池缩放均基于 **`period_amount`**。该额度不负责换算 ETF 价格、100 份一手或券商费用。

### 4.3 执行日

交易日历来自 `exchange-calendars` 的 XSHG 官方日历；行情仓只用于确认 T-1 收盘数据是否已就绪。日历不可用时 fail closed，暂停当期额度，不用普通工作日猜测。

| 频率 | 规则 |
|------|------|
| 每日 | 每个交易日执行 |
| 每周 | 目标星期；休市则顺延至**本周**下一交易日；本周无剩余交易日则本周跳过 |
| 每月 | 目标自然日（1–28）；休市则顺延至**本月**下一交易日；本月无则顺延至**下月首个交易日** |

- 「今天」= 用户自然日（`date.today()`）；策略信号仍用 **T-1** 收盘。  
- 非执行日：`execution_today=false`，分配额度清零并注明原因；`reduce` 止盈不受影响。
- `next_execution_date` 始终严格晚于今天；是否今天执行只由 `execution_today` 表示。
- 看板额外字段：`period_amount`、`execution_today`、`next_execution_date`、`buy_frequency`。

---

## 5. 看板信息架构

首页「今日操作清单」展示：

1. 信号日期（T-1）  
2. 是否今日执行定投 / 下一执行日  
3. 月预算、本期基准分配、今日合计分配
4. 各标的操作卡片（本期分配额度 / 倍数 / 硬否决 / **合成依据与子策略 reason**）
5. 警告（现金池、非执行日、mock 等）

策略对比页额外展示：**估值 / 均线 / 止盈 / 合成的计算依据说明**，以及各标的当日信号 `reason`（可展开对照倍数表）。

「信号复盘」页（`/review`）按时间线回看已缓存信号日的决策计数，以及之后 5 / 20 / 60 日与至今的等权前瞻收益。历史快照在设置/对账变更时保留，仅失效当日 T-1 缓存。

---

## 6. 设置页交互

- 月定投总预算（元）  
- 定投频率：每日 / 每周 / 每月  
- 每周：选择星期（一至五）  
- 每月：选择日期（1–28）  
- 可支配定投储备（现金池）  
- 现金池显式启用开关（默认关闭；启用后 0 表示空池，pool_factor 归 0 暂停买入）
- 成长仓空头策略选择框：防守（空头硬停）/ 追收益（软降频，倍数 0–1 可调）
- 目标权重、持仓、硬否决与止盈开关  
- 保存时逐字段校验非负金额与合理上限，并校验短均线小于长均线、武装线低于清仓线、目标权重标的合法且合计为 1；422 错误在对应字段或组合旁显示
- 保存后写入 Mongo（或 memory）并落盘 `backend/data/user_state.json`

---

## 7. 数据与技术约束

| 项 | 说明 |
|----|------|
| 前端 | Next.js |
| 后端 | FastAPI |
| 行情 | SQLite `market.db` |
| 设置/持仓 | Mongo 或 mongomock + `user_state.json` |
| 信号日 | T-1，不用未收盘点 |
| 无行情标的 | 看板跳过该标的卡片并在警告区列出「以下标的无行情数据」 |
| 盈亏口径 | 看板盈亏采用**含分红总回报**：`(市值 + 累计分红 − 成本) / 成本`；市值优先用持仓市值，否则用未复权 ETF 收盘价。对账登记分红后计入 `dividends_received`，避免除权日把账面盈亏打成亏损 |

### 非目标

- 不下单券商对接  
- 不上 ERP / RSI / KDJ 主信号  
- 不恢复网格/再平衡主策略（`grid.py` / `rebalance.py` 仅为遗留实验代码，引擎不调用）  

---

## 8. 风险声明

可用 `PYTHONPATH=backend backend/.venv/bin/python backend/scripts/backtest_strategy.py` 对本地 SQLite 时点数据做 T-1 信号、下一交易日执行的离线回测，并与相同现金流和调度的等权固定定投比较。**回测复用实盘策略管线**（含估值新鲜度 fail-safe、硬否决、清仓锁与底仓逻辑），不再与实盘行为脱节。

支持三种进阶检查：

1. `--oos-start YYYY-MM-DD`：样本外分割，输出样本内 / 样本外分段指标（样本外 XIRR 计入期初已投入资本）；  
2. `--sensitivity`：单因子敏感性扫描，每次只动一个旋钮，量化每个参数的方向与幅度；  
3. `--growth-bear-policy soft`：成长仓空头排列由硬否决改为软降频（与设置页同一开关，`--variant growth-bear-soft` 为等价简写）。

实测结论（2020-01 至 2026-08，月度定投 ¥1 万）：择时策略收益低于等权固定定投（XIRR 3.25% vs 5.64%），换来更低波动与回撤；**成长仓空头硬否决是最大收益拖累**（软降频变体 XIRR 4.51%、TWR 25.5%，仍低于固定定投但差距大幅收窄）。策略需自行确认定位：防守型（低波动/低回撤，接受收益让渡）或追收益（软降频/提高暂停分位/提高估值权重，均需样本外证据）。

策略信号仅供学习与个人研究参考，不构成投资建议；历史分位与均线不能预测未来收益。回测受样本期、代理估值及 ETF 上市日期限制，策略仍需要严格的样本外证据。

---

## 9. 对账 / 复盘 / 提醒（v5）

### 9.1 持仓对账登记

- 目标：让看板持仓状态回归真实，避免「建议」与「实际执行」脱节导致现金池、止盈 stage、成本失真。
- 交易类型：`deposit`（入金，增加可支配储备）、`buy`（买入，花费金额 / 成交价）、`sell`（卖出，份额或比例 / 成交价）、`dividend`（分红，现金入账或红利再投）。
- 规则：
  - 买入：加权平均成本 = 原有成本 + 本次花费；`shares += amount/price`；`cash -= amount`（超出可支配储备则拒绝，提示先入金）。
  - 卖出：`shares -= shares|ratio×shares`；`cash += 份额×price`；平均成本不变。
  - 分红：`dividends_received += amount`；现金分红 `cash += amount`，红利再投则 `shares += amount/price`（成本不变）。
  - 每笔写入 `Portfolio.trades` 台账（含 applied_at），持久化并失效看板缓存。
- 盈亏口径升级：**含分红总回报** —— `profit_ratio = (市值 + 累计分红 − 成本) / 成本`，消除分红除权造成的盈亏失真。

### 9.2 信号复盘时间线

- `GET /api/signals/history` 返回最近 N 个已缓存信号日快照（`signals_daily`），含执行日、本期分配、动作计数、警告。
- 每个历史信号日附带**等权前瞻收益**：基于各标的指数 T-1 收盘，分别计算 5 / 20 / 60 日与至今（各自标的日历，未上市标的跳过）。
- `/review` 页按时间线展示，用于复盘「当时为什么这样判断、之后表现如何」。

### 9.4 组合再平衡（研究开关，默认关闭）

**定位**：实验特性，适合偏防守用户做研究对比；**看板默认不启用**。全样本回测显示：相对无再平衡基线，温和参数能压回撤，但 XIRR 有稳定代价，尚未通过「默认启用」门槛，仍需样本外验证。

#### 研究参数（仅 `--rebalance` 回测开启时生效）

| 旋钮 | 研究值 | 说明 |
|------|--------|------|
| 触发阈值 | **15%** | `\|actual_weight − target_weight\| ≥ 15%` |
| 低配加码上限 | **1.15×** | `factor = 1 + deficit / target_weight`，再封顶 |
| 超配减仓系数 | **0.02** | `reduce_ratio = 0.02 × excess / actual_weight` |

全样本对照（月定投 1 万、5bp 成本，相对无再平衡基线）：XIRR 约 −0.34pp，最大回撤约改善 1.7pp。激进参数（5% / 0.5 / 1.5×）会把收益打没，已弃用。

#### 触发规则

- 实际持仓权重由「持仓份额 × T-1 ETF 收盘价 / 组合总市值」计算。
- 仅当总持仓市值 > 0 且 `rebalance_enabled` 时才改信号；无持仓或默认关闭则跳过。
- 硬否决、清仓止盈锁优先于再平衡，不因低配强行解锁。

#### 低配侧（actual < target）

- 仅当该标的基础信号为 `buy` 时才加码。
- 最终倍数 = `min(max_mult, base_mult × factor)`，`factor` 上限 1.15×。
- `rebalance_reason` 说明偏差幅度与加码系数。

#### 超配侧（actual > target）

- 若基础信号不是 `reduce`，则产出再平衡减仓建议。
- 若已有止盈 `reduce` 信号，则保留原建议，不叠加再平衡减仓。
- `rebalance_reason` 说明偏差幅度与建议减仓比例。

#### 展示与可追溯性

- 开启后再平衡时，今日操作清单（`/`）显示实际仓位与偏差；偏差 ≥15% 时超配红色、低配蓝色高亮。
- 有再平衡建议时，卡片内显示独立超配/低配提示（`rebalance_reason`）。
- 回测：`python backend/scripts/backtest_strategy.py --rebalance` 对比有/无再平衡的 XIRR 与最大回撤。

### 9.3 提醒推送

- 设置页配置 Webhook 地址（Server酱 / 钉钉 / 企业微信等 JSON 端点）与开关。
- worker 每日同步后触发 `should_notify`：
  - `notify_on_execution`：执行日且存在买入或减仓建议时推送；
  - `notify_on_signal_change`：今日各标的行为签名（symbol→action）相对上一缓存信号日变化时推送。
- 推送为 `POST {webhook}`，载荷含日期、执行日、本期分配、买卖/暂停清单与警告；失败仅记日志，不影响主流程。
